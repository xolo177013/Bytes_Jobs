# API base URL — the Vite dev proxy forwards /api to localhost:8000
# Do NOT change this for local development
VITE_API_URL=/api

# For production deployment, update to your actual API URL:
# VITE_API_URL=https://api.yourdomain.com/api
